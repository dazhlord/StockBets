ports:
  - port: 8080
    onOpen: open-browser
tasks:
  - name: Build Contracts
    init: cd hardhat && npm i
    command: yarn build
  - name: Front End
    init: cd frontend && npm install
    command: npm run serve
  - name: Airnode
    init: yarn download-airnode
    command: yarn start-airnode
